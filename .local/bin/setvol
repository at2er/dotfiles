#!/bin/sh

SHIFT=5

[ -n "$2" ] && SHIFT=$2

usage() {
	cat << EOF
usage: volume [-hudlgm] [VOLUME]
  -h: usage
  -u: up
  -d: down
EOF
}

case "$1" in
	"-h") usage ;;
	"-u") wpctl set-volume @DEFAULT_SINK@ ${SHIFT}%+ ;;
	"-d") wpctl set-volume @DEFAULT_SINK@ ${SHIFT}%- ;;
esac
